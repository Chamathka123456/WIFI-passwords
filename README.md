# WIFI-passwords
give WIFI password you connected
This project contains a Windows batch script that lists all saved Wi‑Fi profiles on a system and displays their stored passwords (if available).
It uses the built‑in netsh command and works on Windows systems where the user has administrator privileges.

⚠️ Warning: This script reveals Wi‑Fi passwords in plain text. Use it only on systems and networks you own or are authorized to manage.
**How to Use

Copy the script into a file named:

wifi-passwords.bat


Right‑click the file and select Run as administrator

The script will:

Enumerate all saved Wi‑Fi profiles

Display each network name

Show the stored password (if present)

📄 Sample Output
==============================
Wi-Fi Name: HomeNetwork
Key Content            : MySecurePassword123**
